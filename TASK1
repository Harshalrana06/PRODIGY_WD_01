<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Interactive Navigation Menu</title>
  <style>
    /* Reset & basic styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      padding-top: 60px; /* for space under the fixed navbar */
    }

    /* Navigation bar */
    .navbar {
      position: fixed;
      top: 0;
      width: 100%;
      background-color: rgba(0, 0, 0, 0.8);
      color: white;
      display: flex;
      justify-content: space-around;
      align-items: center;
      padding: 15px 20px;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      z-index: 1000;
    }

    .navbar.scrolled {
      background-color: #333;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
    }

    .nav-item {
      color: white;
      text-decoration: none;
      margin: 0 15px;
      font-size: 18px;
      transition: color 0.3s ease, transform 0.3s ease;
    }

    .nav-item:hover {
      color: #00ccff;
      transform: scale(1.1);
    }

    /* Dummy content to scroll */
    .content {
      height: 2000px;
      background: linear-gradient(#f4f4f4, #ddd);
      padding: 20px;
    }
  </style>
</head>
<body>

  <nav class="navbar" id="navbar">
    <a href="#home" class="nav-item">Home</a>
    <a href="#services" class="nav-item">Services</a>
    <a href="#about" class="nav-item">About</a>
    <a href="#contact" class="nav-item">Contact</a>
  </nav>

  <div class="content">
    <h1 id="home">Home Section</h1>
    <p>Scroll down to see the navigation menu change.</p>

    <h2 id="services" style="margin-top: 600px;">Services Section</h2>
    <p>Some more dummy content here...</p>

    <h2 id="about" style="margin-top: 600px;">About Section</h2>
    <p>Even more dummy content here...</p>

    <h2 id="contact" style="margin-top: 600px;">Contact Section</h2>
    <p>You're at the end!</p>
  </div>

  <script>
    // JavaScript to change navbar style on scroll
    const navbar = document.getElementById("navbar");

    window.addEventListener("scroll", () => {
      if (window.scrollY > 50) {
        navbar.classList.add("scrolled");
      } else {
        navbar.classList.remove("scrolled");
      }
    });
  </script>

</body>
</html>
